import subprocess
import sys
import os

# result= subprocess.run([sys.executable, '-c', "print('this is a test')"], capture_output=True, text=True)

# print(result.stdout)
# print(result.stderr)

# with open ('output.txt', "w") as f:

# output = subprocess.Popen(['ls', '-al'], stdout=subprocess.PIPE, text=True).communicate()[0] 

# print(output)

print(os.listdir())

